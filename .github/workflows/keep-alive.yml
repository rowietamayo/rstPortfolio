name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 0 */3 * *' # Runs every 3 days at midnight UTC
  workflow_dispatch: # Allows manual triggering from the GitHub Actions tab

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Storage CDN
        run: |
          curl -I https://jkpenfuvjsikletzganq.supabase.co/storage/v1/object/public/portfolio-assets/bracket.svg
